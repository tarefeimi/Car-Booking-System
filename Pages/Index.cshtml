@page
@model RentalRazorPages.Pages.MakinaPages.IndexModel
@{
    ViewData["Title"] = "Home page";
}



<section class="jumbotron text-center">
    <div class="container">
        <h1 class="jumbotron-heading">RRP Best Car Rental In Town</h1>
        <p class="lead text-muted">
            Fast & Easy Way To Rent A Car
            A small river named Duden flows by their place and supplies it with the necessary regelialia. It is a paradisematic country, in which roasted parts
        </p>
        <p>
            <a class="btn btn-primary my-2" asp-page="/BookingPages/Create">Book a Car Now</a>
            <a class="btn btn-secondary my-2"  asp-page="/MakinaPages/Index">View All Our Cars</a>
        </p>
    </div>
</section>

<div class="album py-5 bg-light">
    <div class="container">
        <div class="row">
            @foreach (var item in Model.MakinatViewModel.Makina)
            {
                <div class="col-md-4">
                    <div class="card mb-4 box-shadow">
                        <img class="card-img-top" src="~/images/@item.ImageName" width="80" alt="Card image cap" />
                        <div class="card-body">
                            <p class="card-text"> @Html.DisplayFor(m => item.MarkaId)  @Html.DisplayFor(m => item.ModeliId) </p>
                            <div class="d-flex justify-content-between align-items-center">
                                <small class="text-muted">@Html.DisplayFor(m => item.VitiProdhimit)</small>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>


